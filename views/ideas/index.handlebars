{{#each ideas}}
<div class="card card-body mb-2">
  <h4>{{title}}</h4>
  <p>{{details}}</p>
  <a class="btn btn-dark btn-block" href="/ideas/edit/{{id}}">Edit</a>
  <a class="btn btn-danger btn-block mb-2" href="/ideas/delete/{{id}}">Delete</a>
</div>
{{else}}
<p>No video ideas listed</p>
{{/each}}